<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectFolha.B"
             xmlns:VM="clr-namespace:ProjectFolha.ViewModel"
             Title="Página B">
    <ContentPage.BindingContext>
        <VM:VMTeste/>
    </ContentPage.BindingContext>
    <StackLayout>
        <Label Text="Página B" VerticalOptions="Center" HorizontalOptions="Center"/>

        <Button Text="Sincronizar" 
                Command="{Binding GetRHEstadoCivilCommand}"/>
        
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"/>

        <!--<ListView x:Name="ListaTeste"
                  ItemsSource="{Binding RhEstadoCivil}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding EstadoCivil}"
                               Detail="{Binding Descricao20}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->


    </StackLayout>
</ContentPage>